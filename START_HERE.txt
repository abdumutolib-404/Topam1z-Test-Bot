╔══════════════════════════════════════════════════════════════════╗
║                                                                  ║
║   🚀 START HERE - @topam1z_bot Google Cloud Deployment          ║
║   Zero-brain deployment - just copy & paste these commands      ║
║                                                                  ║
╚══════════════════════════════════════════════════════════════════╝

┌──────────────────────────────────────────────────────────────────┐
│  STEP 1: Download All Files                                     │
│  ↓ Click each file above to download                            │
└──────────────────────────────────────────────────────────────────┘

You need these 3 files:
  • config.py
  • yt_dlp_tools.py
  • moviebox_tools.py

┌──────────────────────────────────────────────────────────────────┐
│  STEP 2: Upload to Google Cloud VM                              │
│  Run this on your LOCAL machine:                                │
└──────────────────────────────────────────────────────────────────┘

cd ~/Downloads  # or wherever you downloaded files

gcloud compute scp config.py YOUR-VM-NAME:~/topam1z-bot/
gcloud compute scp yt_dlp_tools.py YOUR-VM-NAME:~/topam1z-bot/
gcloud compute scp moviebox_tools.py YOUR-VM-NAME:~/topam1z-bot/

┌──────────────────────────────────────────────────────────────────┐
│  STEP 3: Deploy on VM                                           │
│  Run this on your VM (after SSH):                               │
└──────────────────────────────────────────────────────────────────┘

gcloud compute ssh YOUR-VM-NAME

# Now on VM:
cd ~/topam1z-bot

# Fix cookies
sudo docker compose down
rm -rf cookies.txt *.txt
touch cookies.txt www.youtube.com_cookies.txt www.instagram.com_cookies.txt

# Rebuild with fixes
sudo docker compose build --no-cache
sudo docker compose up -d

# Watch it work
sudo docker compose logs -f bot

┌──────────────────────────────────────────────────────────────────┐
│  ✅ SUCCESS LOOKS LIKE:                                          │
└──────────────────────────────────────────────────────────────────┘

bot-1 | ╔══════════════════════════════════════════╗
bot-1 | ║   ASSISTANT BOT  ·  @topam1z_news        ║
bot-1 | ╚══════════════════════════════════════════╝
bot-1 | Cookies: cookies.txt OK (0B)
bot-1 | 🤖 Bot is live!

NO "IsADirectoryError" → You're done! 🎉

┌──────────────────────────────────────────────────────────────────┐
│  ⚠️  TROUBLESHOOTING                                             │
└──────────────────────────────────────────────────────────────────┘

Still seeing "IsADirectoryError"?

sudo docker compose down
ls -la *.txt
# If any show "drwxr" (directory), remove them:
rm -rf cookies.txt
touch cookies.txt
sudo docker compose up -d

┌──────────────────────────────────────────────────────────────────┐
│  📝 OPTIONAL: Apply Admin Panel Fixes                           │
│  (Only if you use admin panel ads/broadcast)                    │
└──────────────────────────────────────────────────────────────────┘

See ADMIN_PANEL_PATCHES.txt for instructions.

These are small patches - just add "return" in 3 places
and replace one function in shared.py.

┌──────────────────────────────────────────────────────────────────┐
│  🎯 TEST YOUR BOT                                                │
└──────────────────────────────────────────────────────────────────┘

1. Send YouTube link → Should download video ✓
2. Send music file → Should identify song ✓
3. Search movie → Should download ✓
4. /admin → Admin panel works ✓

═══════════════════════════════════════════════════════════════════

That's it! Your bot is now fixed and running on Google Cloud. 🚀

For detailed docs, see README.md
For copy-paste commands, see GCLOUD_DEPLOY_SIMPLE.md

═══════════════════════════════════════════════════════════════════
