# ── @topam1z_bot — pinned for fast, reliable Docker builds ────────────
# All versions tested and confirmed compatible

# Telegram bot framework
python-telegram-bot[job-queue]==22.7

# Video/audio downloading — upgraded to latest at build time
yt-dlp==2026.3.17

# Async HTTP client — pinned to avoid yarl/multidict resolver loops
aiohttp==3.11.18
aiosignal==1.3.2
frozenlist==1.5.0
multidict==6.1.0
yarl==1.18.3

# PostgreSQL async driver
asyncpg==0.30.0

# Music recognition
shazamio==0.8.1

# HTTP client (for upload chain + moviebox)
httpx==0.27.2
httpcore==1.0.7
h11==0.14.0

# MovieBox movie/series search
moviebox-api==0.3.5
beautifulsoup4==4.12.3

# Load .env file in local development
python-dotenv==1.0.1
