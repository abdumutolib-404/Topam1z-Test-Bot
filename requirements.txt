# ── @topam1z_bot dependencies ─────────────────────────────────────────────
# All packages installed by Docker — do NOT run pip install manually.

# Telegram bot framework
python-telegram-bot[job-queue]>=21.0,<23.0

# Video/audio downloading (always upgraded to latest at build time)
yt-dlp>=2026.3.13

# Async HTTP client
aiohttp>=3.11.0

# PostgreSQL async driver
asyncpg>=0.30.0

# Music recognition
shazamio>=0.8.1

# Load .env file in local development
python-dotenv>=1.0.0

# MovieBox — search & download movies/TV series (no API key)
moviebox-api>=0.3.5
httpx>=0.27.0
