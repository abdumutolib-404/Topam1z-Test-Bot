[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "python -u bot.py"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
numReplicas = 1          # CRITICAL: exactly one instance — bot uses polling not webhooks
